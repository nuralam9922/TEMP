# Codex Studio

A modern split-pane coding editor with a resizable layout, autosave with configurable delay, search/replace tools, formatting actions, live preview, and downloadable snippets.

## Run locally

```bash
python3 -m http.server 4173
```

Then open <http://localhost:4173>.
